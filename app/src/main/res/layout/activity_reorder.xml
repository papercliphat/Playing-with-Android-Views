<!--
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context="com.robosto.robosto.ReorderActivity">

    <TextView
        android:text="I'm going to put in ability to drag and drop clubs which will be represented as bars.  If multiple bars are under the same header then I'll pull data for both of them

        //Interactive UI for changing club order
        Database of clubs is saved android SQL and API db
        Create preferred order column, save as 100 when club created
        User the pull from order probably programmed in
            if all are 100, it will pull with some order - probably slower
        The first time you create order, update them
        save it in my database and theirs, phone puts it in preferred order"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />

</RelativeLayout>
-->
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/mainLayout"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ListViewAnimations" >

    <com.robosto.robosto.DynamicListView
        android:id="@+id/listview"
        android:background="#0000"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

</RelativeLayout>